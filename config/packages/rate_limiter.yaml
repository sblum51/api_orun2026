framework:
    rate_limiter:
        password_reset:
            policy: 'token_bucket'
            limit: 5
            rate: { interval: '15 minutes', amount: 5 }

when@test:
    framework:
        rate_limiter:
            password_reset:
                policy: 'token_bucket'
                limit: 1000
                rate: { interval: '1 minute', amount: 1000 }
